# Haiku variant: Merge conflicts

Output only:

- Next git commands (add, commit/continue)
- One-line reminder: `MERGE_HEAD` ⇒ merge not done
- One `git diff` line for bulk `--ours`/`--theirs` review if relevant
