# Opus variant: Merge conflict resolution

Expand reasoning for the user:

- Explain why `git diff` can be empty while a merge is unfinished (`MERGE_HEAD`).
- Walk through merge-base vs `origin/main` diffs when reviewing bulk resolutions.
- Tie verification steps to this repository’s documented commands when known.

Still follow the same phase order as `SKILL.md`; avoid unrelated refactors.
