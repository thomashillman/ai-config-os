# Test Writer — Balanced Variant (Sonnet)

Generate unit + integration tests covering:
- Happy path (main use case)
- Edge cases (null, empty, boundary values, large inputs)
- Error states (exceptions, invalid inputs)
- Integration points (mocked dependencies)

Target 80%+ line coverage.
