{
  "name": "ai-config-os-executor",
  "version": "1.0.0",
  "description": "Cloudflare Worker executor (Phase 1: Cloudflare-only tools)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.101.0"
  }
}
