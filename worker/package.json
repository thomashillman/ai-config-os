{
  "name": "ai-config-os-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for ai-config-os skill distribution",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "check:test-types": "tsc -p tsconfig.tests.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "typescript": "^5.7.2",
    "vitest": "^2.0.0",
    "wrangler": "^3.101.0"
  }
}
